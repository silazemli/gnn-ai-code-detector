[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "gnn-ai-code-detector"
version = "0.1.0"
description = "Graph neural network for AI code detection"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]